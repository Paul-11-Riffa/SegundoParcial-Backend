# ======================================
# CONFIGURACIÓN DE ENTORNO - EJEMPLO
# ======================================
# IMPORTANTE: Copia este archivo como .env y configura tus valores reales
# NO subas el archivo .env a git (está en .gitignore)

# ======================================
# DJANGO CONFIGURATION
# ======================================
# IMPORTANTE: En producción, genera una nueva SECRET_KEY única y segura
# Puedes generarla con: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=tu-secret-key-super-segura-aqui-cambiar-en-produccion

# DEBUG: True para desarrollo, False para producción
DEBUG=False

# ALLOWED_HOSTS: Dominios permitidos (separados por comas)
# En producción con Render: tu-app.onrender.com
# Si tienes dominio propio: tu-dominio.com,www.tu-dominio.com
ALLOWED_HOSTS=localhost,127.0.0.1,tu-app.onrender.com

# ======================================
# CORS CONFIGURATION
# ======================================
# Orígenes permitidos para CORS (separados por comas)
# En producción: https://tu-frontend.com,https://www.tu-frontend.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ======================================
# DATABASE CONFIGURATION
# ======================================
# PostgreSQL en la nube (Neon, AWS RDS, etc.)
DB_NAME=nombre_base_datos
DB_USER=usuario_base_datos
DB_PASSWORD=contraseña_base_datos
DB_HOST=host.base.datos.com
DB_PORT=5432

# ======================================
# EMAIL CONFIGURATION
# ======================================
# Configuración para envío de emails (Gmail, SendGrid, etc.)
EMAIL_HOST_USER=tu-email@gmail.com
EMAIL_HOST_PASSWORD=tu-app-password-de-gmail

# ======================================
# STRIPE CONFIGURATION (Pagos)
# ======================================
# Claves de Stripe para procesamiento de pagos
# Obtén tus claves en: https://dashboard.stripe.com/apikeys
STRIPE_PUBLIC_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxx
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxx

# ======================================
# FIREBASE CONFIGURATION (Notificaciones Push)
# ======================================
# Ruta al archivo de credenciales de Firebase
# Descarga desde: Firebase Console > Project Settings > Service Accounts
FIREBASE_CREDENTIALS_PATH=./firebase-credentials.json

# ======================================
# GOOGLE CLOUD CONFIGURATION (Speech-to-Text)
# ======================================
# Ruta al archivo de credenciales de Google Cloud
# Descarga desde: Google Cloud Console > IAM & Admin > Service Accounts
GOOGLE_CLOUD_CREDENTIALS_PATH=./google-cloud-credentials.json

# ======================================
# NOTAS IMPORTANTES PARA PRODUCCIÓN:
# ======================================
# 1. Cambia DEBUG=False
# 2. Genera una nueva SECRET_KEY única
# 3. Configura ALLOWED_HOSTS con tu dominio real
# 4. Usa base de datos de producción (no SQLite)
# 5. Configura CORS_ALLOWED_ORIGINS con tu frontend real
# 6. Usa claves de Stripe en modo LIVE (no test)
# 7. Asegúrate de tener los archivos de credenciales Firebase y Google Cloud
# 8. Configura HTTPS en tu servidor
# 9. Configura variables de entorno en tu servidor (no uses archivo .env en producción)
# 10. Ejecuta: python manage.py collectstatic antes de desplegar
